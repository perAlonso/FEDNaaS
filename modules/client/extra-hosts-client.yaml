version: '3.3'
services:
  client0:
    extra_hosts:
      combiner: ${combiner_ip}
      reducer: ${reducer_ip}
  client1:
    extra_hosts:
      combiner: ${combiner_ip}
      reducer: ${reducer_ip}
